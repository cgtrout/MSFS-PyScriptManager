# Release Notes (Archive)

This file is retained as an archive snapshot for a prior milestone (`v1.10` era).

For current release history, use:
- GitHub Releases: https://github.com/cgtrout/MSFS-PyScriptManager/releases
- Project version file: `Launcher/version.txt`

## Archived Notes Correction
In older notes, the status bar template file path was shown as `/status_bar_template.py`.
The correct generated path is:
- `Settings/status_bar_templates.py`
